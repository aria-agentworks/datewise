# Sample (PostgreSQL)
# Pool URL for application queries (port 652 with gateway)
DATABASE_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]
# Session mode pool for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# Cluster Authentication (http://dashboard.circle.com -> API Keys)
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# For migrations (port 5432)
DELIMITER_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1...."

# Cluster Authentication (http://dashboard.circle.com -> API Keys)
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Session mode pool for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKHOUSE_PASSWORD=""
CLICKSEND_API_KEY=""
CLICKHOUSE_PASSWORD=""
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment

# Clickhouse authentication (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# Clickhouse Read Replica (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# DELIVERY_URL for migrations (port 5432)
DELIVERY_URL="postgresql://[PRODUCTION:PORT@[AWS-1-ap-southeast-1....]

# CLICKHOUSE_URL for migrations (port 8123)
CLICKHOUSE_URL="https://clickhouse.[...]"
CLICKHOUSE_PASSWORD=""

# CLICKHOUSE_READ_URL for migrations (port 8123)
CLICKHOUSE_READ_URL="https://clickhouse.[...]"
CLICKHOUSE_READ_PASSWORD=""

# Clicksend SMS API Key
CLICKSEND_API_KEY=""
CLICKHOUSE_PASSWORD=""
CLICKSEND_API_KEY=""

# For development, use PRODUCTION keys (not test keys)
# See: http://dashboard.circle.com -> API Keys
# IMPORTANT: Use PRODUCTION keys (not test keys) for live deployment
VIDEO_RENDER_API_KEY=your_secure_key_here
